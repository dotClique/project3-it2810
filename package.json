{
  "name": "project3-it2810",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prepare": "npx prisma generate",
    "prepareuse": "npx prisma generate && node ./index.ts  && npx prisma db push"
  },
  "author": "",
  "dependencies": {
    "@prisma/client": "^3.2.1",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.10.3",
    "prisma": "^3.2.1",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "prisma": {
    "schema": "./project3-common/src/prisma/schema.prisma"
  }
}
